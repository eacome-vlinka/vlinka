---
import Layout from 'layouts/Layout.astro';
import Button from 'components/Button.astro';
---

<Layout
  title="关于我们"
  description="了解VLINKA - 专注于AI驱动的智能音视频会议设备和解决方案的专业厂商。"
  navDarkMode={true}
>
  <!-- Page Header -->
  <section class="page-header" data-theme-section="dark">
    <div class="container">
      <h1>关于 VLINKA</h1>
      <p class="page-description">智能音视频会议解决方案专家</p>
    </div>
  </section>

  <!-- About Section -->
  <section class="section section-about" data-bg-section="light">
    <div class="container">
      <div class="about-grid">
        <div class="about-content">
          <h2>我们的使命</h2>
          <p>VLINKA致力于为全球企业和机构提供先进的AI驱动音视频会议设备和解决方案。我们深信，高效的沟通是成功的基石，而出色的会议体验是实现高效沟通的关键。</p>
          <p>通过持续的技术创新和对品质的执着追求，我们打造了一系列涵盖波束成形扬声器、AI阵列麦克风、天花板麦克风、智能会议系统和视频条的全方位产品线，满足从小型会议室到大型会议厅的各种需求。</p>
        </div>
        <div class="about-image">
          <div class="image-placeholder">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round">
              <rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect>
              <line x1="8" y1="21" x2="16" y2="21"></line>
              <line x1="12" y1="17" x2="12" y2="21"></line>
            </svg>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Values Section -->
  <section class="section section-values" data-bg-section="light-tint">
    <div class="container">
      <h2>核心价值</h2>
      <div class="values-grid">
        <div class="value-card">
          <div class="value-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polygon points="12 2 2 7 12 12 22 7 12 2"></polygon>
              <polyline points="2 17 12 22 22 17"></polyline>
              <polyline points="2 12 12 17 22 12"></polyline>
            </svg>
          </div>
          <h3>技术创新</h3>
          <p>持续投入研发，将AI技术与音视频领域深度融合，打造领先的智能会议产品。</p>
        </div>

        <div class="value-card">
          <div class="value-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
            </svg>
          </div>
          <h3>品质至上</h3>
          <p>严格的品质管控体系，确保每一款产品都达到行业领先的性能标准。</p>
        </div>

        <div class="value-card">
          <div class="value-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
              <circle cx="9" cy="7" r="4"></circle>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
              <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
            </svg>
          </div>
          <h3>客户为本</h3>
          <p>深入了解客户需求，提供定制化的解决方案和专业的售后服务支持。</p>
        </div>

        <div class="value-card">
          <div class="value-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"></circle>
              <line x1="2" y1="12" x2="22" y2="12"></line>
              <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"></path>
            </svg>
          </div>
          <h3>全球视野</h3>
          <p>面向全球市场，为世界各地的企业和机构提供专业的会议解决方案。</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="section section-stats" data-bg-section="dark" data-theme-section="dark">
    <div class="container">
      <div class="stats-grid">
        <div class="stat-item">
          <span class="stat-value">13+</span>
          <span class="stat-label">产品系列</span>
        </div>
        <div class="stat-item">
          <span class="stat-value">5</span>
          <span class="stat-label">产品类别</span>
        </div>
        <div class="stat-item">
          <span class="stat-value">100+</span>
          <span class="stat-label">企业客户</span>
        </div>
        <div class="stat-item">
          <span class="stat-value">24/7</span>
          <span class="stat-label">技术支持</span>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="section section-cta" data-bg-section="light">
    <div class="container">
      <div class="cta-content">
        <h2>与我们合作</h2>
        <p>无论您是需要单一产品还是完整的会议解决方案，我们都能为您提供专业的支持</p>
        <Button text="联系我们" link="/cn/contact" variant="primary" size="xl" />
      </div>
    </div>
  </section>
</Layout>

<style>
  .page-header {
    padding-top: calc(var(--nav-bar-height) + var(--section-padding));
    padding-bottom: var(--section-padding);
    background: linear-gradient(135deg, var(--color-dark) 0%, var(--color-dark-tint) 100%);
    position: relative;
  }

  .page-header::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 20% 80%, rgba(var(--color-primary-rgb), 0.15) 0%, transparent 40%);
    pointer-events: none;
  }

  .page-header .container {
    position: relative;
    z-index: 2;
  }

  .page-header h1 {
    color: var(--color-white);
    margin-bottom: 0.25em;
  }

  .page-description {
    font-size: 1.1em;
    color: var(--color-text-light);
  }

  /* About Grid */
  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3em;
    align-items: center;
  }

  @media screen and (max-width: 860px) {
    .about-grid {
      grid-template-columns: 1fr;
      gap: 2em;
    }
  }

  .about-content h2 {
    margin-bottom: 1em;
  }

  .about-content p {
    font-size: 1em;
    line-height: 1.8;
    margin-bottom: 1em;
  }

  .about-image .image-placeholder {
    aspect-ratio: 4/3;
    background: linear-gradient(135deg, var(--color-dark) 0%, var(--color-dark-tint) 100%);
    border-radius: calc(var(--border-radius) * 2);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .image-placeholder svg {
    width: 4em;
    height: 4em;
    stroke: rgba(var(--color-white-rgb), 0.3);
  }

  /* Values Grid */
  .section-values h2 {
    text-align: center;
    margin-bottom: 2em;
  }

  .values-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5em;
  }

  .value-card {
    padding: 1.5em;
    background-color: var(--color-white);
    border-radius: calc(var(--border-radius) * 1.5);
    border: 1px solid var(--color-border);
    text-align: center;
  }

  .value-icon {
    width: 3em;
    height: 3em;
    margin: 0 auto 1em;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    border-radius: 50%;
  }

  .value-icon svg {
    width: 1.5em;
    height: 1.5em;
    stroke: var(--color-white);
  }

  .value-card h3 {
    font-family: 'Manrope', sans-serif;
    font-size: 1.1em;
    font-weight: 600;
    color: var(--color-dark);
    margin-bottom: 0.5em;
  }

  .value-card p {
    font-size: 0.9em;
    color: var(--color-text);
    line-height: 1.6;
  }

  /* Stats */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2em;
    text-align: center;
  }

  @media screen and (max-width: 768px) {
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .stat-value {
    font-family: 'Manrope', sans-serif;
    font-size: 3em;
    font-weight: 700;
    color: var(--color-primary);
    display: block;
    line-height: 1;
  }

  .stat-label {
    font-size: 1em;
    color: var(--color-text-light);
    margin-top: 0.5em;
    display: block;
  }

  /* CTA */
  .section-cta {
    text-align: center;
  }

  .cta-content h2 {
    margin-bottom: 0.5em;
  }

  .cta-content p {
    color: var(--color-text);
    margin-bottom: 1.5em;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }
</style>
